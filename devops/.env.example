# Environment overrides for Docker Compose (devops)
# Copy this file to devops/.env to customize. These values are safe for local development.

MONGO_INITDB_ROOT_USERNAME=appuser
MONGO_INITDB_ROOT_PASSWORD=apppass
MONGO_INITDB_DATABASE=realtime_stock
